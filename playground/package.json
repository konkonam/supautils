{
    "type": "module",
    "scripts": {
        "lint:fix": "bunx eslint --fix --no-ignore ./generated",
        "gen": "supautils gen:schemas --url=postgresql://postgres:postgres@localhost:54322/postgres --tables=public.* --outputDir=./generated/cli && bun lint:fix",
        "gen:dev": "supautils gen:schemas --url=postgresql://postgres:postgres@localhost:54322/postgres --tables=public.* --outputDir=./generated/cli && bun lint:fix",
        "gen:lib": "bun index.ts"
    },
    "dependencies": {
        "supabase": "^2.22.6",
        "supautils": "link:supautils"
    }
}